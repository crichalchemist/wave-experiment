# Detective LLM — deployment requirements
# PyTorch installed separately in setup.sh (CPU wheel from pytorch.org)

# Inference client (Ollama's /v1 endpoint is OpenAI-compatible)
openai>=1.0.0

# Azure AI Foundry (Claude critic/fallback)
azure-ai-inference>=1.0.0

# HuggingFace training stack
transformers>=4.40.0
trl>=0.8.0
peft>=0.10.0
datasets>=2.18.0
accelerate>=0.28.0

# Graph
networkx>=3.0
kuzu>=0.5.0

# API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Vault / memory
httpx>=0.27.0

# CLI
click>=8.1.0

# NLP
spacy>=3.7.0

# Data
pdfplumber>=0.10.0