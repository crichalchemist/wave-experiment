# Detective LLM — deployment requirements
# PyTorch installed separately in setup.sh (CPU wheel from pytorch.org)

# Inference client (vLLM's /v1 endpoint is OpenAI-compatible)
openai
vllm

# Azure AI Foundry (Claude critic/fallback)
azure-ai-inference

# HuggingFace training stack
transformers
trl
peft
datasets
accelerate

# Graph
networkx
kuzu

# API
fastapi
uvicorn[standard]

# Vault / memory
httpx

# CLI
click

# NLP
spacy

# Data
pdfplumber
