# Detective LLM — Azure GPU deployment requirements
#
# Minimum VM SKU: Standard_NC6s_v3 (6 vCPU, 112 GB RAM, 1× V100 16 GB)
# Recommended:    Standard_NC24s_v3 or Standard_ND40rs_v2 for full context length
#
# Install order:
#   1. pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   2. pip install -r requirements-azure.txt
#
# The setup.sh in this directory handles the CPU (Ollama) path for L-series VMs.
# Use this file when GPU is available and vLLM inference throughput is required.

# Inference engine (GPU)
vllm

# Azure AI Foundry (Claude critic / fallback)
azure-ai-inference

# HuggingFace training stack
transformers
trl
peft
datasets
accelerate

# Graph
networkx
kuzu

# API
fastapi
uvicorn[standard]

# Vault / memory
httpx

# CLI
click

# NLP
spacy

# Data
pdfplumber
